# Pauta (piloto)

Monorepo. `api/` é FastAPI + SQLAlchemy, `web/` é React + Vite.

Hoje funciona: equipes, criar pauta em rascunho, propor item, listar itens.
Não existe ainda: votação, transições de estado da pauta, decisões, notificações, API pública.
